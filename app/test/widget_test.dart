// Placeholder — real tests are in test/models/, test/providers/, test/widgets/
// This file exists to satisfy flutter test's default discovery.

void main() {
  // Tests are organized in subdirectories:
  //   test/models/        — unit tests for model serialization
  //   test/providers/     — unit tests for PackageFormNotifier
  //   test/widgets/       — widget tests for key widgets
}
